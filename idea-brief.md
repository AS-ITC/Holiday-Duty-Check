# ไอเดีย: อัปเดต data.js ของแอป "ตรวจเวรวันหยุด" เป็นข้อมูลจริงของบริษัท SS + เพิ่ม 2 ฟอร์มบันทึกใหม่

> **สถานะ (2026-09-19):** ส่วน "พื้นที่/โซน" (areas) และ "ประเภทปัญหา" (issueTypes) แก้ `data.js` เสร็จแล้ว — ใช้งานได้จริงทันที เพราะพบว่ารายชื่อผู้ตรวจ (department) กับพื้นที่ตรวจ (location) **ไม่ได้ผูกกัน** ในตัวแอป (department ใช้แค่กรองหาชื่อเร็วขึ้น หน้าจอเลือกพื้นที่แสดงทุกโซนให้ทุกคนเหมือนกันเสมอ) — รายชื่อผู้ตรวจของ SS จริงๆ ยังไม่มี แต่ **ไม่ใช่ตัวบล็อกการใช้งานอีกต่อไป** เป็นแค่เรื่องดีถ้ามีเพิ่มเพื่อความสะดวก ส่วน 2 ฟอร์มใหม่ (เบิกของสโตร์ / ขนย้ายเครื่องจักร) ยังไม่ได้ทำ เป็น "ทำก็ได้ไม่ทำก็ได้" ตามที่ยืนยันไว้

## ปัญหา / ที่มา
ข้อมูลแผนก/พื้นที่-โซน (AC, CS, EN, HR, PE, PF1, PF2, WH) ที่อยู่ใน `data.js` ตอนนี้เป็นแค่ **ข้อมูลตัวอย่างช่วงทดลองสร้างระบบ** ไม่ใช่ข้อมูลจริง ตอนนี้ได้รับไฟล์พื้นที่/จุดตรวจจริงของบริษัท **SS** มาแล้ว (`งาน ผจก เวรวันหยุด 1.xlsx`) ต้องเอาไปแทนที่ข้อมูลตัวอย่างทั้งหมด เพื่อให้แอปพร้อมใช้งานจริง นอกจากนี้ระหว่างตรวจเวร ทีมยังต้องบันทึกเหตุการณ์ "เบิกของสโตร์" และ "ขนย้ายเครื่องจักร/อะไหล่ออกนอกพื้นที่" ที่เกิดขึ้นเป็นครั้งคราว ซึ่งปัจจุบันไม่มีช่องทางบันทึกในระบบเดียวกัน

## ผู้ใช้งาน
ทีมตรวจเวรวันหยุดของบริษัท SS ใช้งานประมาณ 1-2 คน/สัปดาห์

## Input
ไฟล์ Excel `งาน ผจก เวรวันหยุด 1.xlsx` มี 3 ชีท:
1. **Zone 1-13** — จุดตรวจพื้นที่ทั้งหมดของ SS แบ่งเป็น 13 โซนใหญ่ (Zone1-Zone13) ย่อยลงไปอีกรวม 17 จุดตรวจ (เช่น "Zone1 - Pilot Plant ชั้น 5...") แต่ละจุดมีรายการ "เสี่ยง"/ประเภทปัญหาของตัวเอง รวมทั้งไฟล์มี **14 ประเภทปัญหาที่ไม่ซ้ำกัน** (เตรียมไว้แล้วที่ `SS_Area.csv` และ `SS_IssueTypes.json`)
2. **เบิกของสโตร์** — แบบฟอร์มบันทึกการเบิกของสโตร์ (ฝ่าย/แผนกผู้ขอเบิก, ชื่อผู้ขอเบิก, รายการ, จำนวน, สาเหตุ, ผู้อนุมัติ)
3. **ขนย้ายเครื่องจักร** — แบบฟอร์มบันทึกการขนย้ายเครื่องจักร/อะไหล่ออกนอกพื้นที่โรงงาน (ฝ่าย/แผนกผู้ขอ, ประเภทเครื่องจักร/อะไหล่, รายละเอียด, จำนวน, สาเหตุ, ผู้อนุมัติ, หัวหน้าชุด รปภ ประจำวันหยุด)

ชีท 2 และ 3 มีรายชื่อ "ฝ่าย/แผนก" ของบริษัทในเครือทั้งหมด (ไม่ใช่แผนกของผู้ตรวจ SS) ใช้เป็น dropdown ผู้ขอเบิก/ผู้ขอขนย้ายเท่านั้น — เตรียมไว้แล้วที่ `SS_Org_List.csv`

**ยังขาด:** รายชื่อผู้ตรวจ (members/คน) ของทีม SS จริง — ยังไม่ได้รับไฟล์นี้ ต้องรอก่อนจึงจะใช้งานแอปได้ครบ

## Output ที่อยากได้
แอป "ตรวจเวรวันหยุด" ตัวเดิม (repo `duty-check-repo-offline`) ที่:
- ใช้ข้อมูลพื้นที่/จุดตรวจจริงของ SS แทนข้อมูลตัวอย่างเดิมทั้งหมด
- มีประเภทปัญหาครบ 14 แบบตามไฟล์จริง
- มี 2 ฟอร์มบันทึกใหม่ (เบิกของสโตร์ / ขนย้ายเครื่องจักร-อะไหล่) ให้กรอกเพิ่มเติมระหว่างตรวจเวรได้ (ไม่บังคับ)

## ต้องมี (Must have)
- **ลบข้อมูลตัวอย่างเดิมทิ้งทั้งหมด** (departments AC/CS/EN/HR/PE/PF1/PF2/WH และ areas/issueTypes ตัวอย่าง) ใน `data.js`
- แทนที่ด้วยข้อมูลจริงของ SS: ใช้ **Zone1-Zone13 เป็น location ใหม่**, รายละเอียด/ห้องแต่ละจุดเป็น **zone ย่อย** (รวม 17 จุด, ตามลำดับในไฟล์)
- แทนที่ `issueTypes` ด้วยรายการ 14 ประเภทปัญหาไม่ซ้ำกันจากไฟล์ — **ใช้ list เดียวกันข้ามทุกโซน** (ไม่ต้องแยกตามพื้นที่ย่อย — เพื่อไม่ต้องแก้โครงสร้างแอปเพิ่ม)
- ตรวจพื้นที่ตามโซนต้องกรอกให้ครบทุกจุด (บังคับ — เหมือนพฤติกรรมเดิมของแอป)
- เพิ่มฟีเจอร์ใหม่ 2 แบบฟอร์ม (กรอกแบบไม่บังคับ แล้วแต่มีเหตุการณ์จริงในกะนั้นหรือไม่):
  1. **บันทึกเบิกของสโตร์**: ฝ่าย/แผนกผู้ขอเบิก (dropdown จาก org list), ชื่อผู้ขอเบิก, รายการ, จำนวน, สาเหตุ, ผู้อนุมัติ
  2. **บันทึกขนย้ายเครื่องจักร/อะไหล่**: ฝ่าย/แผนกผู้ขอ (dropdown จาก org list), ประเภท (เครื่องจักร/อะไหล่), รายละเอียด, จำนวน, สาเหตุ, ผู้อนุมัติ, หัวหน้าชุด รปภ ประจำวันหยุด
- ดรอปดาวน์ "ฝ่าย/แผนกผู้ขอ" ในฟอร์มใหม่ 2 อัน **เป็นคนละ list กับแผนกผู้ตรวจ (location)** — ใช้ org list ทั้งบริษัทจาก `SS_Org_List.csv`

## น่าจะมี (Nice to have)
- (ไม่มีที่ระบุเพิ่มเติมในตอนนี้)

## ข้อจำกัด
- ความอ่อนไหวของข้อมูล: ไม่มีข้อมูลอ่อนไหว (ชื่อพื้นที่/โซน, รายชื่อฝ่าย/แผนกทั่วไป)
- ต้อง offline / ออนไลน์: offline เหมือนเดิม (single file, no database — สถาปัตยกรรมเดิมรองรับอยู่แล้ว)
- แพลตฟอร์ม: มือถือ + เดสก์ท็อป (เหมือนเดิม)
- **บล็อกอยู่ที่:** ยังไม่มีรายชื่อผู้ตรวจ (members) ตัวจริงของทีม SS — เขียน data.js ส่วน location/zone/issueTypes ได้เลยตอนนี้ แต่ส่วน members ต้องรอไฟล์เพิ่มก่อนถึงจะสมบูรณ์
- 2 ฟอร์มใหม่เป็นฟีเจอร์ใหม่จริง (ไม่ใช่แค่แก้ data.js) ต้องผ่านขั้นออกแบบหน้าตา/scope ที่ spec-writer ก่อนลงมือเขียนโค้ด

## แนวทาง/เครื่องมือที่อาจเอามาใช้ต่อได้
- repo เดิม `duty-check-repo-offline` (`index.html` + `data.js` + `README.md`)
- ไฟล์อ้างอิงที่เตรียมไว้แล้วจากไฟล์ Excel ที่ส่งมา:
  - `SS_Area.csv` — รายการ location/zone/order (17 จุดตรวจ) พร้อมใส่ใน `data.js` ส่วน `areas`
  - `SS_IssueTypes.json` — รายการ 14 ประเภทปัญหาไม่ซ้ำกัน พร้อมใส่ใน `data.js` ส่วน `issueTypes`
  - `SS_Org_List.csv` — รายชื่อฝ่าย/แผนกทั้งบริษัท ใช้เป็น master data ใหม่สำหรับ dropdown ผู้ขอในฟอร์มเบิกของ/ขนย้าย
- อาจใช้สกิล `itail-design-system` ตอนออกแบบหน้าตา 2 ฟอร์มใหม่ให้เข้าธีมเดิมของแอป
